<div class="post">
<h1>Listing bills</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bills.each do |bill| %>
      <tr>
        <td><%= link_to 'Anzeigen', bill %></td>
        <td><%= link_to 'Bearbeiten', edit_bill_path(bill) %></td>
        <td><%= link_to 'Löschen', bill, method: :delete, data: { confirm: 'Sind Sie sicher?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bill', new_bill_path %>
</div>