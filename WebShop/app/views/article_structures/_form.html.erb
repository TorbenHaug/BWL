<%= form_for(@article_structure) do |f| %>
  <% if @article_structure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article_structure.errors.count, "error") %> prohibited this article_structure from being saved:</h2>

      <ul>
      <% @article_structure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :deleted_at %><br>
    <%= f.datetime_select :deleted_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
