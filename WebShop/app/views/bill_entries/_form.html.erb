<div class="post">
<%= form_for(@bill_entry) do |f| %>
  <% if @bill_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill_entry.errors.count, "error") %> prohibited this bill_entry from being saved:</h2>

      <ul>
      <% @bill_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>